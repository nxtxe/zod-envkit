{
  "NODE_ENV": {
    "description": "Runtime mode",
    "example": "development",
    "required": true,
    "group": "Runtime"
  },
  "API_SECRET": {
    "description": "Server-only secret for API or internal calls",
    "example": "change-me-in-production",
    "required": true,
    "group": "Server"
  },
  "LOG_LEVEL": {
    "description": "Logging level",
    "example": "info",
    "required": false,
    "group": "Runtime"
  }
}
